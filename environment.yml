name: s3contents
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pip

  # Base conda deps
  - pylint
  - flake8
  - black
  - isort
  - twine
  - pytest
  - pytest-cov

  # This project conda deps

  - pip:
    # This project pip deps
    - requests
    - boto3
    - s3fs
    - gcsfs
    - mock
